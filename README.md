# 📰 NewsBot Intelligence

NewsBot Intelligence is a machine learning project that classifies news articles by category using natural language processing and logistic regression.

## 🔧 Features
- Cleans messy or HTML-based datasets
- Handles inconsistent CSV or JSON structures
- Uses TF-IDF vectorization and Logistic Regression
- Saves and reloads trained models
- Predicts news categories interactively

## 🚀 Run in Google Colab
1. Open the notebook in Google Colab.
2. Upload your dataset (CSV with `text` and `label` or `category` columns).
3. Run all cells.
4. Test your own news headlines!

## 📁 Output Files
- `clean_news_data.csv` — cleaned dataset
- `newsbot_model.pkl` — trained model
- `tfidf_vectorizer.pkl` — TF-IDF vectorizer
